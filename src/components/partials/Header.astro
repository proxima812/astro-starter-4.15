---
import Nav from '../Nav'

const { container_max_w } = Astro.props
---

<header class="px-5 mt-5">
	<!-- <div class:list={`${container_max_w ?? "max-w-6xl"} container px-5`}> -->
		<!--  -->
		<Nav client:load/>
</header>
